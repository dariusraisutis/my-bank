import LoginComponent from "../components/LoginComponent";


const Login = () => {
    return <>
        <LoginComponent 
            handleSubmit={() => {}} 
            onNameChange={() => {}} 
            onPasswordChange={() => {}} 
        />        
    </>;
}

export default Login;